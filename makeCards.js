// const makeCards = (title, freeSpaceText, promptSet, numberOfCards) => {
//   const cards = [];
//   for (i=0; i<numberOfCards; i++){
//     promptSet.sort(() => (Math.random() > .5) ? 1 : -1);
//     const card = new Card(title, freeSpaceText,  promptSet.slice(0,24));
//     cards.push(card);
//   }
//   return cards;
// }